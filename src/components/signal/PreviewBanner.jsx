import React from "react";

/** Launch preview notice, in the same typographic register as the section labels. */
export default function PreviewBanner() {
  return (
    <p className="mb-10 text-[11px] uppercase tracking-[0.24em] text-[#1B2430]">
      Launch preview - your first report is free
    </p>
  );
}